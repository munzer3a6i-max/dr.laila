[build]
  publish = "."
  functions = "netlify/functions"

# keep one client-side path (/api/*) whichever host this is deployed to
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
